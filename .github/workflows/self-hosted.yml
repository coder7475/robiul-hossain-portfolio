name: Portfolio Deployment

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Update and upgrade apt packages
        run: |
          sudo apt-get update
          sudo apt-get upgrade -y
