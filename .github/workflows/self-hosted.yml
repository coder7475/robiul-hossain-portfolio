name: Portfolio Deployment

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Update and upgrade apt packages
        env:
          SUDO_PASSWORD: ${{ secrets.SUDO_PASSWORD }}
        run: |
          echo $SUDO_PASSWORD | sudo apt update && sudo apt upgrade -y
