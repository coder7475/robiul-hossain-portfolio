name: CD

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: fahad-first-runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Change Directory
        run: |
        cd /home/admin/robiul-hossain-portfolio
        pwd

      - name: Run linting
        run: npm run lint

      - name: Deploy
        run: ./.scripts/deploy.sh
